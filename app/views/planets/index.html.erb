<h1>Book a trip to space!</h1>
<ul>
  <% @planets.each do |planet| %>
    <li>
      <%= link_to "#{planet.name}", planet_path(planet) %>
    </li>
  <% end %>
</ul>
<%= link_to "List a new planet to visit", new_planet_path %>
